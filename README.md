# ⚠️ Moved — this repo is archived

cp-adapter now lives in the main repo, alongside the enclave it bridges to:

### 👉 https://github.com/aoraki-labs/fidrouter/tree/main/cp-adapter

Same license (Apache-2.0), same code, plus `THREAT_MODEL.md` and verified-lane group
gating. Keeping it next to the enclave means everything a partner is asked to run or audit
is in one place. Nothing here is maintained any more.
